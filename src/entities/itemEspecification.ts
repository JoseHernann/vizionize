type itemEspecification = {
  name: string;
  stock: number;
  precioUnit: number;
  precioVent: number;
  proveedor: number;
  category: number;
  image: string;
};

export default itemEspecification;
